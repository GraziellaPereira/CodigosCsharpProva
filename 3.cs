// Online C# Editor for free
// Write, Edit and Run your C# code using C# Online Compiler

using System;

namespace DiasentreDatas
{
    class Program
    {
        static void Main()
        {
            DateTime dI, dF;

            Console.WriteLine("Digite a data inicial (formato dd/MM/yyyy): ");
           di = DateTime.Parse(Console.ReadLine());
        
            Console.WriteLine("Digite a data final (formato dd/MM/yyyy): ");
            df = DateTime.Parse(Console.ReadLine());
  

            TimeSpan diferenca = dF - dI;

            Console.WriteLine("Distância de dias: " + diferenca.Days);
            Console.ReadLine();
        }
    }
}

// Online C# Editor for free
// Write, Edit and Run your C# code using C# Online Compiler

using System;

namespace DiasentreDatas
{
    class Program
    {
        static void Main()
        {
            DateTime dI, dF;

            Console.WriteLine("Digite a data inicial (formato dd/MM/yyyy): ");
           di = DateTime.Parse(Console.ReadLine());
        
            Console.WriteLine("Digite a data final (formato dd/MM/yyyy): ");
            df = DateTime.Parse(Console.ReadLine());
  

            TimeSpan diferenca = dF - dI;

            Console.WriteLine("Distância de dias: " + diferenca.Days);
            Console.ReadLine();
        }
    }
}